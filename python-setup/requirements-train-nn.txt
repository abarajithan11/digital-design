# Keep the CPU wheel index available so Linux does not pull a CUDA-enabled torch
# build. PyPI remains available for Brevitas and its other dependencies.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==1.26.4
torch==2.12.1
torchvision==0.27.1
brevitas==0.12.1
